
class ModelDAO(object):
    def __init__(self,subscription_key,date_time,file_name,model_id,model_url):
        self.subscriptionKey = subscription_key
        self.dateTime = date_time
        self.fileName = file_name
        self.modelId = model_id
        self.modelURL = model_url

